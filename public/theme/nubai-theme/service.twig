{% extends 'partials/_master.twig' %}

{% block title %}
    {{ service.title }}
{% endblock %}

{% block main %}

    <h1>{{ service.title }}</h1>
    <img src="{{ service.image|thumbnail(320, 240) }}">
    {{ service.body }}
    <p><a href="{{ path('listing_locale', { 'contentTypeSlug': 'services' }) }}">{% trans %}link.to.services.page{% endtrans %}</a></p>

{% endblock main %}