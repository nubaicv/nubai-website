<!-- Sidebar -->
<aside>
    <div class="w3-bar" style="list-style-type: none">
        {%  for category in config.get('taxonomies/categories/options') %}
            <div class="w3-bar-item">
                <a href="{{ path('taxonomy_locale', {'taxonomyslug': 'category', 'slug': category|slug }) }}">{{ category|trans }}</a>
            </div>
        {% endfor %}
    </div>
</aside>
<!-- End Sidebar -->
